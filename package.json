{
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel watch serve",
    "build": "gulp",
    "watch:gulp": "gulp watch",
    "watch:webpack": "webpack --watch",
    "watch": "npm-run-all --parallel watch:webpack watch:gulp",
    "serve": "http-server -c-1 ./docs/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "picomodal": "^3.0.0",
    "ractive": "^0.8.7",
    "rx": "^4.1.0",
    "sanitize.css": "^4.1.0",
    "three": "^0.83.0"
  },
  "devDependencies": {
    "babel-core": "^6.21.0",
    "babel-loader": "^6.2.10",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-es2015": "^6.18.0",
    "gulp": "^3.9.1",
    "html-loader": "^0.4.4",
    "http-server": "^0.9.0",
    "npm-run-all": "^3.1.2",
    "webpack-stream": "^3.2.0"
  }
}
